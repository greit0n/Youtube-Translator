# Secrets — YouTube auth cookies + HuggingFace token. NEVER commit.
helper/cookies.txt
cookies.txt
helper/hf_token.txt
hf_token.txt

# Helper runtime artifacts
helper/*.log
helper/cache/
helper/__pycache__/
**/__pycache__/
*.pyc

# Temp audio windows (also live in the OS temp dir, but just in case)
helper/cache/*.json
*.wav

# Whisper model downloads (large)
*.bin
models/

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
